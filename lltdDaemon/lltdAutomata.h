#ifndef LLTD_DAEMON_AUTOMATA_WRAPPER_H
#define LLTD_DAEMON_AUTOMATA_WRAPPER_H

/*
 * Compatibility wrapper: the protocol core lives in `lltdResponder/`.
 * OS integration must not leak into `lltdResponder/` sources.
 */

#include "../lltdResponder/lltd_automata.h"

#endif

